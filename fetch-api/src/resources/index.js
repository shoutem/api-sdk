import extensions, { EXTENSIONS } from './extensions';
export { extensions, EXTENSIONS };

import shortcuts, { SHORTCUTS } from './shortcuts';
export { shortcuts, SHORTCUTS };

import Resource, { JsonApiResource, fetchResource } from './Resource';
export { Resource, JsonApiResource, fetchResource };
